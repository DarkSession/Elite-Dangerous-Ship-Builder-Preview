<?xml version="1.0" encoding="UTF-8"?>
<!--
  The three top-level screens. `/` is not listed: it redirects to `/ships`, and
  a sitemap entry for a redirect is an entry for a page that does not exist.

  This file is also the route list the deployment publishes as real addresses.
  Pages would otherwise serve each of these from `404.html`, and a crawler drops
  a 404 whatever the body says — so the deploy step reads the addresses below
  and writes `<route>.html` for each, which answers 200 with no redirect. Adding
  a `<loc>` here publishes it; there is no second list to keep in step.

  Hull pages (`/ships/<symbol>`) are absent, and their absence is deliberate
  rather than forgotten: the set of hulls belongs to the Almanac, and a
  hand-written list of hull symbols here would be the private copy of package
  data constitution II forbids. Listing them means a generator that reads the
  installed package, which is a build step. Recorded as the highest-value
  follow-up in specs/011-interface-foundations/design/search-visibility.md.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://sb.edct.dev/ships</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://sb.edct.dev/build</loc>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>https://sb.edct.dev/builds</loc>
    <changefreq>monthly</changefreq>
    <priority>0.5</priority>
  </url>
</urlset>
